<core:FragmentDefinition
        xmlns="sap.m"
        xmlns:core="sap.ui.core"
        xmlns:viz="sap.viz.ui5.controls"
        xmlns:vizData="sap.viz.ui5.data"
        xmlns:vizFeed="sap.viz.ui5.controls.common.feeds">
    <Panel id="donutPanel" headerText="{chart>/title}" class="thDonutPanel sapUiResponsiveMargin" width="auto">
        <viz:VizFrame id="donutChart" vizType="donut" width="100%" height="20rem"
                      uiConfig="{applicationSet:'fiori'}"
                      vizProperties="{
                          plotArea: { dataLabel: { visible: true, type: 'value' } },
                          legend:   { visible: true },
                          title:    { visible: false },
                          tooltip:  { visible: true }
                      }">
            <viz:dataset>
                <vizData:FlattenedDataset data="{chart>/data}">
                    <vizData:dimensions>
                        <vizData:DimensionDefinition name="Destination &amp; Currency" value="{chart>name}"/>
                    </vizData:dimensions>
                    <vizData:measures>
                        <vizData:MeasureDefinition name="Amount" value="{chart>value}"/>
                    </vizData:measures>
                </vizData:FlattenedDataset>
            </viz:dataset>
            <viz:feeds>
                <vizFeed:FeedItem uid="size" type="Measure" values="Amount"/>
                <vizFeed:FeedItem uid="color" type="Dimension" values="Destination &amp; Currency"/>
            </viz:feeds>
        </viz:VizFrame>
    </Panel>
</core:FragmentDefinition>
